# ThinkPad machine-specific configs

Place hardware-specific items here: dock/display profiles, hwdb tweaks, udev rules, scripts that reference ThinkPad hardware IDs, and any files that should not be applied on other machines.

Generic configs should live in `config/` and be linked into user `~/.config/` by the installer. Monitor profiles and dock scripts should require explicit user approval before applying.
