#!/bin/bash

# solace:summary=Launch a TUI command in the default terminal with Solace styling
# solace:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.solace.$(basename $1) -e "$1" "${@:2}"
