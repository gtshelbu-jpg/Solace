#!/bin/bash

# solace:summary=Launch a TUI or focus an existing terminal window for it
# solace:args=<command> [args...]

APP_ID="org.solace.$(basename "$1")"
LAUNCH_COMMAND="solace-launch-tui $@"

exec solace-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
