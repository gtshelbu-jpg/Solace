#!/bin/bash

# solace:summary=Finish the installation of Solace with items that can only be done after logging in.
# solace:requires-sudo=true

set -e

FIRST_RUN_MODE=~/.local/state/solace/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$SOLACE_PATH/install/first-run/battery-monitor.sh"
  bash "$SOLACE_PATH/install/first-run/recover-internal-monitor.sh"
  bash "$SOLACE_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$SOLACE_PATH/install/first-run/firewall.sh"
  bash "$SOLACE_PATH/install/first-run/dns-resolver.sh"
  bash "$SOLACE_PATH/install/first-run/gnome-theme.sh"
  bash "$SOLACE_PATH/install/first-run/swayosd.sh"
  bash "$SOLACE_PATH/install/first-run/gtk-primary-paste.sh"
  bash "$SOLACE_PATH/install/first-run/elephant.sh"
  solace-hook-install post-update "$SOLACE_PATH/install/first-run/install-voxtype.hook"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$SOLACE_PATH/install/first-run/welcome.sh"
  bash "$SOLACE_PATH/install/first-run/wifi.sh"
fi
