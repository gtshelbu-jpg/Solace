#!/bin/bash

# solace:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/solace/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! solace-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
